info:
  name: update
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseURL}}/products/2"
  body:
    type: json
    data: |-
      {
        "name" : "Feijao",
        "price": 20.00

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
