info:
  name: save
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/products"
  body:
    type: json
    data: |-
      {
        "name" : "Feijao",
        "price": 10.00

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
